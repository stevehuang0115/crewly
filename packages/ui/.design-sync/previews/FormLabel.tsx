import React from 'react';
import { FormLabel } from '@crewly/ui';

export const Plain = () => <FormLabel htmlFor="x">Project folder</FormLabel>;
export const Required = () => <FormLabel htmlFor="y" required>Team name</FormLabel>;
